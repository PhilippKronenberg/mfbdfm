% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backcast.R
\name{run_fcast}
\alias{run_fcast}
\title{Fit the multi-factor model at a given evaluation date}
\usage{
run_fcast(
  flows,
  stocks,
  target,
  date,
  dataset_used,
  q = 2,
  p = 1,
  length_sample = 1000,
  burn_in = 1000,
  thinning = 1,
  stochastic_volatility = TRUE,
  serial_correlation = TRUE,
  extend = 0.5,
  ncores = NULL,
  control = NULL,
  output_dir = NULL
)
}
\arguments{
\item{flows}{Named list of \code{ts} objects containing \code{target}.}

\item{stocks}{Named list of \code{ts} objects.}

\item{target}{Character, name of the target series in \code{flows}.}

\item{date}{Numeric (decimal time), evaluation date; the factor is cut
at this date.}

\item{dataset_used}{Character, dataset label used as sub-directory
when saving.}

\item{q}{Integer, number of factors, passed to \code{\link[=fcast_dfm]{fcast_dfm()}}.}

\item{p}{Integer, number of lags in the factor VAR.}

\item{length_sample}{Integer, number of posterior draws to keep.}

\item{burn_in}{Integer, number of initial draws to discard.}

\item{thinning}{Integer, keep every \code{thinning}-th draw after burn-in.}

\item{stochastic_volatility}{Logical, passed to \code{\link[=ind_dfm]{ind_dfm()}} (currently
without effect there).}

\item{serial_correlation}{Logical, model serial correlation in the
measurement errors. If \code{FALSE}, the autocorrelations are fixed near zero.}

\item{extend}{Numeric, years by which to extend the dataset past its end
before estimating, passed to \code{\link[=fcast_dfm]{fcast_dfm()}}. \strong{Required for this function to
nowcast at all}, and the reason it defaults to a non-\code{NULL} value here.
At a real-time evaluation date the target's last observation is one or two
quarters old, so the quarter actually being nowcast lies beyond the data;
without extending, \code{\link[=fcast_dfm]{fcast_dfm()}} produces values only over the observed
span and every result is an in-sample fit rather than a forecast. The
default of half a year covers the current quarter and the next.}

\item{ncores}{Integer or \code{NULL}, passed to \code{\link[=fcast_dfm]{fcast_dfm()}} to parallelise
the rotation step.}

\item{control}{Optional settings from \code{\link[=dfm_control]{dfm_control()}}, passed to
\code{\link[=fcast_dfm]{fcast_dfm()}}. Use \code{dfm_control("fcast_dfm", strict = TRUE)} to run the
rotation as specified in the online appendix.}

\item{output_dir}{Directory to save the fit to, or \code{NULL} (default) to
skip saving. When given, the fit is saved as
\code{file.path(output_dir, dataset_used, "fit_<date>.Rda")}.}
}
\value{
Invisibly, the windowed \code{fcast_dfm} fit object.
}
\description{
Runs \code{\link[=fcast_dfm]{fcast_dfm()}} at one evaluation date and windows the factor and
nowcast output to it. The multi-factor counterpart of \code{\link[=run_wai_adj]{run_wai_adj()}},
with the same arguments, the same file layout and the same
no-side-effects-by-default rule, so the two can be driven by the same
backcasting loop.
}
\details{
Note what \code{target} does and does not do here. \code{\link[=fcast_dfm]{fcast_dfm()}} estimates every
series jointly and \code{target} only selects which nowcast is surfaced at the top
level -- unlike \code{\link[=run_wai_adj]{run_wai_adj()}}, where the target is what identifies the
factor. Results for every series remain in \code{ncst} and \code{data_hf} on the
returned object.

\code{q} is the setting that has no counterpart in \code{\link[=run_wai_adj]{run_wai_adj()}}: the
single-factor model has exactly one factor by construction, so a
multi-factor sweep varies \code{q} where the WAI sweep varies the dataset.
}
\examples{
\donttest{
# Short chain on the shipped data; a real evaluation uses the defaults.
data(data_ch_dataset_test)
target <- "ch.seco.gdp.real.gdp.ssa"
flows <- lapply(data_ch_dataset_test$flows[c(target, "SWISSMI")],
                stats::window, start = 2021)
stocks <- lapply(data_ch_dataset_test$stocks[1:2],
                 stats::window, start = 2021)
set.seed(1)
fit <- run_fcast(flows = flows, stocks = stocks, target = target,
                 date = 2023, dataset_used = "example",
                 q = 2, length_sample = 20, burn_in = 5)
fit$nowcast
}

}
\seealso{
\code{\link[=run_wai_adj]{run_wai_adj()}} for the single-factor equivalent, \code{\link[=fcast_dfm]{fcast_dfm()}}
for the model itself.

Other model fitting functions: 
\code{\link{dfm_memory}()},
\code{\link{fcast_dfm}()},
\code{\link{ind_dfm}()}
}
\concept{model fitting functions}
