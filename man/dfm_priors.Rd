% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{dfm_priors}
\alias{dfm_priors}
\title{Specify the prior distributions for a dynamic factor model}
\usage{
dfm_priors(
  model = c("ind_dfm", "fcast_dfm"),
  type = c("default", "uninformative", "informative"),
  ...
)
}
\arguments{
\item{model}{Character, which model the priors are for: \code{"ind_dfm"} or
\code{"fcast_dfm"}. The two models take different priors, and passing a set
built for one to the other is an error.}

\item{type}{Character, the overall strength of the tunable priors:
\code{"default"} (the published values), \code{"uninformative"} (minimal prior
information, letting the data dominate) or \code{"informative"} (more
shrinkage). Does not affect structural priors -- see Details.}

\item{...}{Named overrides for individual priors, each a list of the
entries shown by printing the returned object, e.g.
\code{sigma_other = list(c0 = 3, d0 = 1e-3)}. Naming a structural prior is
allowed but warns.}
}
\value{
An object of class \code{"dfm_priors"}: a named list with an entry per
prior, plus \code{model}, \code{type}, and a \code{structural_modified} flag recording
whether any identifying prior was overridden.
}
\description{
Builds the prior specification passed to \code{\link[=ind_dfm]{ind_dfm()}} or \code{\link[=fcast_dfm]{fcast_dfm()}}. The
defaults reproduce each model's published prior exactly, so
\code{dfm_priors(model)} changes nothing; the object exists so that priors can be
inspected and varied deliberately rather than edited in the sampler source.
}
\section{Structural versus tunable priors}{
Some priors are not tuning knobs -- they \emph{are} the model's identification,
and changing them changes what the estimates mean:

\describe{
\item{\code{ind_dfm}}{The target series' measurement-error variance
(\code{sigma_target}, prior sample size \code{t} and scale \code{t * 1e-3}) and its
serial correlation (\code{rho_target}, prior variance \code{1e-9}) are what force
the augmented target to reproduce the observed series, which is what
makes the factor interpretable as the target's growth rate. Loosening
them dissolves that anchoring.}
\item{\code{fcast_dfm}}{The loading prior (\code{lambda}) must stay diffuse
(\code{B0 = 1e9}); restricting it conflicts with the post-hoc rotation that
identifies the model.}
}

\code{type} therefore moves \strong{tunable priors only}. Structural priors can still
be set, by naming them explicitly in \code{...}, but doing so emits a warning:
the resulting fit is a different model from the published one.
}

\section{Priors that depend on the data}{
Several defaults scale with the sample length \code{t} or the number of lags
\code{p}, which are not known until the data have been prepared. Those entries
are stored as \code{NULL}, meaning "use the model's rule", and are resolved
inside the sampler. Supplying a number instead overrides the rule with a
literal value.
}

\section{Interpreting the inverse-gamma priors}{
Variance priors are inverse-gamma, written so that the posterior update is
\code{c1 = c0 + t} and \code{d1 = d0 + sum(residuals^2)}. So \code{c0} is a prior sample
size in pseudo-observations, \code{d0} is the sum of squares they carry, and
\code{d0 / c0} is roughly the prior's guess at the variance.
}

\examples{
# the defaults reproduce the published priors
dfm_priors("ind_dfm")

# let the data speak more
dfm_priors("ind_dfm", type = "uninformative")

# override one tunable prior
dfm_priors("fcast_dfm", sigma = list(c0 = 3, d0 = 1e-6))

}
\seealso{
\code{\link[=ind_dfm]{ind_dfm()}}, \code{\link[=fcast_dfm]{fcast_dfm()}}
}
\concept{model specification}
