% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/date-utils.R
\name{is_crisis_period_fcast}
\alias{is_crisis_period_fcast}
\title{Flag target quarters falling into the Eckert et al. (2025) crisis periods}
\usage{
is_crisis_period_fcast(period)
}
\arguments{
\item{period}{Numeric vector of target quarters in decimal time, e.g.
\code{2020.25} for 2020Q2. This is the \code{period} column of the evaluation panel,
not the nowcast date.}
}
\value{
Logical vector, \code{TRUE} for quarters inside a crisis window.
}
\description{
The crisis/non-crisis split used in the out-of-sample evaluation of Eckert,
Kronenberg, Mikosch & Neuwirth (2025). Carries the \verb{_fcast} suffix for the
same reason the multi-factor internals do: it is the counterpart of
\code{\link[=is_crisis_period]{is_crisis_period()}} for that paper, and the two are \strong{not} interchangeable.
}
\details{
Two differences from \code{\link[=is_crisis_period]{is_crisis_period()}}, both of which matter:

\describe{
\item{What is classified}{This takes the \strong{target quarter} being nowcast,
as decimal time (\code{2020.25} for 2020Q2). \code{\link[=is_crisis_period]{is_crisis_period()}} takes the
\strong{date the nowcast was made}. In a real-time evaluation those are weeks
or months apart, so the two label different rows.}
\item{Which episodes}{Four windows here -- the global financial crisis, the
euro crisis, the 2015 Swiss franc shock and Covid-19. \code{\link[=is_crisis_period]{is_crisis_period()}}
covers only the first and last.}
}

Measured on the paper's own evaluation panel, the two agree on only 124 of
its 240 crisis dates, and substituting \code{\link[=is_crisis_period]{is_crisis_period()}} does not
reproduce the published figures: crisis RMSFE for the monthly benchmark comes
out at 0.0262 against the 0.0210 this definition gives, which is what the
paper plots.
}
\examples{
# 2020Q1-2021Q3 are crisis quarters; 2019 and 2022 are not
is_crisis_period_fcast(c(2019, 2020, 2021.25, 2022))

}
\references{
Eckert, F., Kronenberg, P., Mikosch, H., & Neuwirth, S. (2025).
Tracking economic activity with alternative high-frequency data.
\emph{Journal of Applied Econometrics}, 40(3), 270-290.
}
\seealso{
\code{\link[=is_crisis_period]{is_crisis_period()}} for the date-based definition used by the
Weekly Activity Index analysis.
}
