% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-data.R
\name{mfbdfm_example_inputs}
\alias{mfbdfm_example_inputs}
\title{Synthetic input bundle for the analytics table builders}
\usage{
mfbdfm_example_inputs(seed = 99)
}
\arguments{
\item{seed}{Integer, the random seed, so a bundle is reproducible. Pass
\code{NULL} to draw from the current state of the RNG instead.}
}
\value{
A named list of 11 elements: nine weekly \code{data.frame}s with \code{time}
and \code{value} columns (three of them also carrying \code{name}), and two quarterly
\code{ts} objects (\code{x_hist_gr_yoy}, \code{x_hist_gr_ann}) standing in for realised GDP
growth.
}
\description{
Builds a small, self-contained \code{inputs} bundle of the shape
\code{\link[=get_insample_fit_table]{get_insample_fit_table()}}, \code{\link[=get_combined_cor_table]{get_combined_cor_table()}} and
\code{\link[=get_insample_error_details]{get_insample_error_details()}} expect, filled with synthetic numbers.
}
\details{
The real bundle is assembled by the scripts under \verb{analysis/5_plots/} from
fitted models and the private real-time GDP vintages, so none of the analytics
functions could previously be demonstrated in a runnable example -- their
examples were unevaluated sketches referring to objects that did not exist,
which meant \verb{R CMD check} never executed them. That is exactly how two
analysis scripts came to read files nothing produced (#59, #63). This exists so
those examples run and are checked.

It is \strong{not} a model of Swiss data. The series are random walks and white
noise of the right shape and frequency; use it to exercise the code paths and
to see the argument shapes, not to interpret numbers.

The same bundle backs the analytics tests, so the examples and the tests
exercise identical input.
}
\examples{
inputs <- mfbdfm_example_inputs()
names(inputs)
head(inputs$tab_gr)

# what it is for
fit_tabs <- get_insample_fit_table("mean", "indicators", inputs = inputs)
fit_tabs$RMSE

}
\seealso{
\code{\link[=get_insample_fit_table]{get_insample_fit_table()}}, \code{\link[=get_combined_cor_table]{get_combined_cor_table()}},
\code{\link[=get_insample_error_details]{get_insample_error_details()}}

Other data preparation functions: 
\code{\link{create_inventory}()},
\code{\link{mfbdfm_data}()},
\code{\link{prepare_data}()}
}
\concept{data preparation functions}
