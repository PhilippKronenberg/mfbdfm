% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory.R
\name{dfm_memory}
\alias{dfm_memory}
\alias{dfm_workers}
\title{Estimate the peak memory of a model fit, and size a parallel run from it}
\usage{
dfm_memory(
  flows = NULL,
  stocks = NULL,
  n = NULL,
  t = NULL,
  s = NULL,
  q = 2,
  p = 1,
  length_sample = 1000,
  extend = 0.5,
  frequency = NULL
)

dfm_workers(..., available_mb = NULL, safety = 0.7, max_workers = NULL)
}
\arguments{
\item{flows, stocks}{Named lists of \code{ts} objects, or an \code{\link[=mfbdfm_data]{mfbdfm_data()}} object
as \code{flows}. Supply these to have the dimensions derived from the data. If
\code{NULL}, give \code{n}, \code{t} and \code{s} directly.}

\item{n}{Integer, number of series. Derived from the data when supplied.}

\item{t}{Integer, number of high-frequency periods \strong{before} \code{extend}.
Derived from the data when supplied.}

\item{s}{Integer, \code{2 * (k - 1)} where \code{k} is the ratio of highest to lowest
frequency. Derived from the data when supplied.}

\item{q}{Integer, number of factors.}

\item{p}{Integer, number of lags in the factor VAR.}

\item{length_sample}{Integer, number of posterior draws \strong{kept} (\code{thinning}
does not enter: it changes how long the chain runs, not how much is
stored).}

\item{extend}{Numeric, the \code{extend} argument the fit will be given, in years.
Lengthens the sample the sampler sees and so the memory it needs.}

\item{frequency}{Integer, observations per year of the highest-frequency
series, used with \code{extend}. Derived from the data when supplied.}

\item{...}{Passed to \code{dfm_memory()}.}

\item{available_mb}{Numeric, memory to plan against, in MB. Defaults to the
machine's currently \strong{free} memory, not its total -- whatever else is
running is not available to the sweep.}

\item{safety}{Numeric in (0, 1], the fraction of \code{available_mb} to commit.
The default 0.7 leaves room for the estimate's ~10\% error, for the
allocator's inability to reuse freed blocks immediately, and for the
process that launched the workers.}

\item{max_workers}{Integer, an upper bound (defaults to the core count).}
}
\value{
\code{dfm_memory()}: estimated peak memory for one fit, in MB (numeric,
length 1). \code{dfm_workers()}: the number of workers (integer, length 1, at
least 1), with the estimate and the budget it used attached as attributes.
}
\description{
\code{dfm_memory()} predicts the peak heap one \code{\link[=fcast_dfm]{fcast_dfm()}} fit will occupy, from
the dimensions of the data and the length of the chain. \code{dfm_workers()} turns
that into the number of parallel workers a machine can actually carry.
}
\details{
Vintage sweeps are parallelised over dates, and the binding constraint is
memory rather than cores: a machine with 16 cores runs out of RAM long before
it runs out of cores, and the failure mode is an opaque
\verb{CHOLMOD error 'out of memory'} from deep inside the sparse solver, hours
into a run. This exists so the worker count can be derived rather than
guessed.
}
\section{The model}{
Peak memory is fitted as

\deqn{a + b \cdot G + c \cdot D}

where \eqn{G} is the footprint of the observation matrix \code{Gmat}
(\verb{q(t-1)n(s+2)} nonzeros at 12 bytes each) and \eqn{D} is the footprint of
the retained draws (\code{length_sample} rows of \verb{nq + pq^2 + 2n + nt + t + s}
doubles). The fitted coefficients say the fit holds about \strong{6.2 live copies
of \code{Gmat}} and \strong{1.9 copies of the draw matrix}, over a fixed \strong{267 MB} of
R and package overhead.

\strong{Memory is linear in \code{q}, not quadratic.} The natural guess is that the
sparse Cholesky of the \code{q(t+s)} square factor precision matrix dominates,
which would scale with \code{q^2}. Measured, it does not: adding that term to the
regression does not improve it, because the \code{Gmat} working set is an order of
magnitude larger. Doubling the factor count costs roughly one extra \code{Gmat}
worth of working set, not four.
}

\section{Calibration}{
Fitted to six fits measured one per fresh R process (the GC high-water mark
is per process, so several in one session would report only their maximum) at
\code{n = 53}, \code{t = 1559}, \code{s = 22}: \verb{q = 1..4} at \code{length_sample = 30}, and
\code{q = 2} at \verb{length_sample = 30, 120, 240}. Adjusted R-squared 0.98, largest
residual 34 MB.

Validated out of sample against a fit on a \strong{different} dataset and a
different version of the code (\code{n = 43}, \code{t = 1464}, \code{q = 2},
\code{length_sample = 200}): 690 MB predicted against 744 MB measured, a 7\% error.

Treat it as accurate to roughly 10\%, which is why \code{dfm_workers()} applies a
safety factor rather than dividing exactly. The constants are specific to
this package's samplers; they were measured on x86_64 Windows and will drift
if the samplers' allocation pattern changes. \code{dev/calibrate-memory.R}
regenerates them.

Calibrated on \code{\link[=fcast_dfm]{fcast_dfm()}}. \code{\link[=ind_dfm]{ind_dfm()}} is a different sampler with no
post-hoc rotation and no packed draw matrix, and has \strong{not} been measured
here.
}

\examples{
# from dimensions
dfm_memory(n = 53, t = 1535, s = 22, q = 2, length_sample = 500)

# the whole point: how many workers fit in 24 GB
dfm_workers(n = 53, t = 1535, s = 22, q = 4, length_sample = 500,
            available_mb = 24 * 1024)

# from the data itself
data(data_ch_dataset_test)
dfm_memory(flows = data_ch_dataset_test$flows,
           stocks = data_ch_dataset_test$stocks,
           q = 2, length_sample = 1000)

}
\seealso{
\code{\link[=fcast_dfm]{fcast_dfm()}}

Other model fitting functions: 
\code{\link{fcast_dfm}()},
\code{\link{ind_dfm}()},
\code{\link{run_fcast}()}
}
\concept{model fitting functions}
