% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/web-export.R
\name{export_wai_web}
\alias{export_wai_web}
\title{Export WAI results as a web-ready CSV and metadata file}
\usage{
export_wai_web(
  fit_path,
  dir = NULL,
  gdp = NULL,
  digits = 4,
  vintage_date = NULL
)
}
\arguments{
\item{fit_path}{Path to a fit \code{.Rda} file containing an object \code{mod}, as
written by \code{\link[=run_wai_adj]{run_wai_adj()}}.}

\item{dir}{Directory to write \code{wai_data.csv} and \code{wai_meta.json} into, or
\code{NULL} (default) to write nothing and only return the data. Created if it
does not exist.}

\item{gdp}{Optional data frame of published GDP, with a \code{time} column plus
either
\itemize{
\item \code{qoq}, \code{yoy} and/or \code{index} — as \code{\link[=gdp_web_series]{gdp_web_series()}} returns, already on
the published scale. This is the intended route.
\item \code{value} — the older single-series shape, written out as \code{gdp_qoq}.
}

\code{time} may be a \code{Date} (as \code{\link[=get_real_time_gdp_vintages]{get_real_time_gdp_vintages()}} returns) or
decimal time in either the \code{\link[=decimal_date_local]{decimal_date_local()}} convention or exact
quarter fractions.

Mind the units if you build this frame yourself:
\code{\link[=get_real_time_gdp_vintages]{get_real_time_gdp_vintages()}} returns \code{"quarterly"} as a log difference
and \code{"annual"} as a fraction, whereas \code{wai_qoq} is an annualised
percentage — unconverted, the two differ on one axis by a factor of roughly
400. \code{\link[=gdp_web_series]{gdp_web_series()}} exists to spare you that conversion.}

\item{digits}{Number of decimal places to round the published series to.
Defaults to 4, which is well past the precision the model supports and
keeps the file small.}

\item{vintage_date}{Optional evaluation/vintage date recorded in the
metadata. Defaults to the last observation date.}
}
\value{
Invisibly, a list with elements \code{data} (the wide data frame) and
\code{meta} (the metadata list). Writes files only when \code{dir} is given.
}
\description{
Turns a fitted WAI model into the two files the public dashboard consumes:
a wide, one-row-per-week CSV of the published series, and a small JSON
sidecar describing the run. Together they are the entire interface between
this package and the web front end — the page performs no computation of its
own, it selects columns.
}
\details{
The CSV has one row per weekly period and these columns, in this order:

\describe{
\item{\code{date}}{ISO date on the 7th/14th/21st/28th convention of \code{\link[=dec2week]{dec2week()}}.}
\item{\code{wai_qoq}, \code{wai_qoq_lo}, \code{wai_qoq_hi}}{Annualised quarter-on-quarter
growth and its 95\% band, from the factor and \code{factor_var}.}
\item{\code{wai_yoy}}{Year-over-year growth of the level index.}
\item{\code{wai_index}}{Level index, rebased to the mean of 2019Q4 = 100.}
\item{\code{gdp_qoq}, \code{gdp_yoy}, \code{gdp_index}}{Published GDP on the same three
measures, placed on the last weekly period of each quarter and empty
elsewhere. Omitted when \code{gdp} is \code{NULL}; which of the three appear
depends on which columns \code{gdp} carries.}
}

\strong{\code{wai_index} and \code{wai_yoy} are published without bands, deliberately.} The
only genuine 95\% credible interval the fit provides is the one on the growth
rate. The level bounds that \code{\link[=extract_wai_data]{extract_wai_data()}} returns in \code{tab_gr_lv_full}
are the index scaled by a \emph{single} period of growth at each bound, not a
compounded level interval, so publishing them next to \code{wai_index} would
present something that looks like a level confidence band and is not one.

Missing values are written as the empty string rather than \code{NA} or \code{NaN}, so
that the front end's parser does not have to know R's spelling of missing.
Numbers use \code{.} as the decimal separator and no thousands separator, rows are
sorted ascending by date, and lines end with \verb{\\n} on every platform.
}
\examples{
\donttest{
# run_wai_adj() is what makes a fit file, so the example produces the file it
# then exports, on a short chain.
data(data_ch_dataset_test)
target <- "ch.seco.gdp.real.gdp.ssa"
flows <- lapply(data_ch_dataset_test$flows[c(target, "SWISSMI")],
                stats::window, start = 2021)
stocks <- lapply(data_ch_dataset_test$stocks[1:2],
                 stats::window, start = 2021)
out <- tempfile(); dir.create(out)

set.seed(1)
run_wai_adj(flows = flows, stocks = stocks, target = target,
            date = 2023, dataset_used = "example",
            length_sample = 20, burn_in = 5, output_dir = out)

web <- export_wai_web(file.path(out, "example", "fit_2023.Rda"),
                      dir = file.path(out, "web"))
head(web$data)
web$meta$n_obs
unlink(out, recursive = TRUE)
}
}
\seealso{
\code{\link[=extract_wai_data]{extract_wai_data()}}, which produces the underlying tables.
}
