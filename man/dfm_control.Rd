% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{dfm_control}
\alias{dfm_control}
\title{Numerical and algorithmic settings for the samplers}
\usage{
dfm_control(model = c("ind_dfm", "fcast_dfm"), strict = FALSE, ...)
}
\arguments{
\item{model}{Character, which model the settings are for: \code{"ind_dfm"} or
\code{"fcast_dfm"}. Determines which knobs are present, since the two samplers
do not share all of them.}

\item{strict}{Logical. If \code{TRUE}, sets the rotation to the published
algorithm -- \code{rotation_criterion = "sum"}, \code{rotation_max_iter} raised, and
failure to converge becomes an \strong{error} rather than a warning. A single
switch for "run it the way the paper specifies". Ignored by \code{\link[=ind_dfm]{ind_dfm()}}.}

\item{...}{Named overrides for individual settings, e.g.
\code{rotation_criterion = "sum"} or \code{sigma_max = 10}. Naming a setting the
chosen model does not have is an error, so a typo surfaces immediately.}
}
\value{
An object of class \code{"dfm_control"}: a named list of settings plus
\code{model} and \code{strict}.
}
\description{
Bundles the numerical and algorithmic knobs that were previously hard-coded
inside the samplers: the rotation stopping rule, the stability bounds that
reject or cap a draw, and two numerical guards. Passing a control object is
\strong{optional} -- omit it and the defaults reproduce the published behaviour
exactly.
}
\section{What is and is not here}{
This holds settings that affect \emph{how} the sampler searches, not \emph{what model}
it fits. Priors live in \code{\link[=dfm_priors]{dfm_priors()}}; model structure is in the arguments
of \code{\link[=ind_dfm]{ind_dfm()}} and \code{\link[=fcast_dfm]{fcast_dfm()}}.

Deliberately absent: initial values, and the numerical conditioning constants
that are not choices (the \code{1e-9} ridge that encodes "effectively zero"
autocorrelation when \code{serial_correlation = FALSE}, and the \code{pi - 1e-16} domain
guard on the Givens angles).
}

\section{The rotation stopping rule}{
Only \code{\link[=fcast_dfm]{fcast_dfm()}} rotates, so \verb{rotation_*} is ignored by \code{\link[=ind_dfm]{ind_dfm()}}.

Appendix E of the online appendix to Eckert et al. (2025), following Assmann,
Boysen-Hogrefe & Pape (2016), specifies convergence when the \strong{sum} of
squared deviations between successive \verb{theta*} falls below \code{1e-9}. The
implementation has always tested the \strong{mean}, which over a packed vector of
several thousand elements is a much weaker requirement, and additionally
capped the loop at five iterations. \code{rotation_criterion = "sum"} restores the
published rule.

Measured on a small two-factor fit, convergence is geometric at roughly an
order of magnitude per iteration: the mean criterion converged at iteration 5
and the sum criterion at iteration 6. Matching the paper therefore costs
about one extra iteration, not the blow-up the difference in thresholds
suggests. Note also that the default cap of 5 sits \emph{exactly} on that
convergence point, so on other data it can bind and truncate the loop -- which
is why a binding cap warns.

\code{rotation_max_iter} and \code{rotation_init_max_iter} are safety valves, not
targets. They default high enough not to bind in practice (100 against 5-7
observed) while still guaranteeing the loop terminates. \code{Inf} is not accepted:
an unbounded loop has no termination guarantee, and a non-converging rotation
should stop and complain rather than run forever.
}

\section{Stability bounds}{
\code{rho_max} bounds the measurement-error autocorrelation. A draw outside it is
redrawn up to \code{rho_max_tries} times, after which \code{rho_fallback} is used. The
others cap or reject a draw for numerical stability: \code{phi_sum_max} and
\code{sigma_max} in \code{\link[=ind_dfm]{ind_dfm()}}, \code{omega_max} in \code{\link[=fcast_dfm]{fcast_dfm()}}.
}

\examples{
dfm_control("fcast_dfm")

# the published rotation rule
dfm_control("fcast_dfm", strict = TRUE)

# or one setting at a time
dfm_control("fcast_dfm", rotation_criterion = "sum", rotation_tol = 1e-10)
dfm_control("ind_dfm", sigma_max = 10)

}
\references{
Assmann, C., Boysen-Hogrefe, J., & Pape, M. (2016). Bayesian analysis of
static and dynamic factor models with an unknown number of factors, and
structural instability. \emph{Journal of Applied Econometrics}, 31(8), 1518-1533.

Eckert, F., Kronenberg, P., Mikosch, H., & Neuwirth, S. (2025).
Tracking economic activity with alternative high-frequency data.
\emph{Journal of Applied Econometrics}, 40(3), 270-290.
}
\seealso{
\code{\link[=ind_dfm]{ind_dfm()}}, \code{\link[=fcast_dfm]{fcast_dfm()}}, \code{\link[=dfm_priors]{dfm_priors()}}

Other model specification: 
\code{\link{dfm_priors}()}
}
\concept{model specification}
