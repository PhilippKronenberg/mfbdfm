% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory.R
\name{dfm_memory}
\alias{dfm_memory}
\alias{dfm_workers}
\title{Estimate the peak memory of a model fit, and size a parallel run from it}
\usage{
dfm_memory(
  flows = NULL,
  stocks = NULL,
  n = NULL,
  t = NULL,
  s = NULL,
  q = 2,
  p = 1,
  length_sample = 1000,
  extend = 0.5,
  frequency = NULL
)

dfm_workers(..., available_mb = NULL, safety = 0.7, max_workers = NULL)
}
\arguments{
\item{flows, stocks}{Named lists of \code{ts} objects, or an \code{\link[=mfbdfm_data]{mfbdfm_data()}} object
as \code{flows}. Supply these to have the dimensions derived from the data. If
\code{NULL}, give \code{n}, \code{t} and \code{s} directly.}

\item{n}{Integer, number of series. Derived from the data when supplied.}

\item{t}{Integer, number of high-frequency periods \strong{before} \code{extend}.
Derived from the data when supplied.}

\item{s}{Integer, \code{2 * (k - 1)} where \code{k} is the ratio of highest to lowest
frequency. Derived from the data when supplied.}

\item{q}{Integer, number of factors.}

\item{p}{Integer, number of lags in the factor VAR.}

\item{length_sample}{Integer, number of posterior draws \strong{kept} (\code{thinning}
does not enter: it changes how long the chain runs, not how much is
stored).}

\item{extend}{Numeric, the \code{extend} argument the fit will be given, in years.
Lengthens the sample the sampler sees and so the memory it needs.}

\item{frequency}{Integer, observations per year of the highest-frequency
series, used with \code{extend}. Derived from the data when supplied.}

\item{...}{Passed to \code{dfm_memory()}.}

\item{available_mb}{Numeric, memory to plan against, in MB. Defaults to the
machine's currently \strong{free} memory, not its total -- whatever else is
running is not available to the sweep.}

\item{safety}{Numeric in (0, 1], the fraction of \code{available_mb} to commit.
The default 0.7 leaves room for the estimate's ~10\% error, for the
allocator's inability to reuse freed blocks immediately, and for the
process that launched the workers.}

\item{max_workers}{Integer, an upper bound (defaults to the core count).}
}
\value{
\code{dfm_memory()}: estimated peak memory for one fit, in MB (numeric,
length 1). \code{dfm_workers()}: the number of workers (integer, length 1, at
least 1), with the estimate and the budget it used attached as attributes.
}
\description{
\code{dfm_memory()} predicts the peak heap one \code{\link[=fcast_dfm]{fcast_dfm()}} fit will occupy, from
the dimensions of the data and the length of the chain. \code{dfm_workers()} turns
that into the number of parallel workers a machine can actually carry.
}
\details{
Vintage sweeps are parallelised over dates, and the binding constraint is
memory rather than cores: a machine with 16 cores runs out of RAM long before
it runs out of cores, and the failure mode is an opaque
\verb{CHOLMOD error 'out of memory'} from deep inside the sparse solver, hours
into a run. This exists so the worker count can be derived rather than
guessed.
}
\section{The model}{
Peak memory is fitted as

\deqn{a + b \cdot G + c \cdot D}

where \eqn{G} is the footprint of the observation matrix \code{Gmat}
(\verb{q(t-1)n(s+2)} nonzeros at 12 bytes each) and \eqn{D} is the footprint of
the retained draws (\code{length_sample} rows of \verb{nq + pq^2 + 2n + nt + t + s}
doubles). The fitted coefficients say the fit holds about \strong{6.3 live copies
of \code{Gmat}} and \strong{1.65 copies of the draw matrix}, over a fixed \strong{270 MB} of
R and package overhead.

\strong{Memory is linear in \code{q}, not quadratic -- and at long chains barely depends
on \code{q} at all.} The natural guess is that the sparse Cholesky of the
\code{q(t+s)} square factor precision matrix dominates, which would scale with
\code{q^2}. Measured, it does not: adding that term to the regression does not
improve it, because the \code{Gmat} working set is an order of magnitude larger.

The \code{q} effect is also much weaker at long chains than at short ones -- each
extra factor costs about 156 MB at 30 retained draws but only about 29 MB at
500. Same cause as the convexity below: once the evaluation phase binds, peak
memory is dominated by the retained draws and the per-series matrices built
from them, whose size is driven by \code{n * t}, while the \code{q}-dependent part of a
draw (\code{nq + pq^2 + t + s}) is negligible beside it. The model keeps \code{q} in the
\code{Gmat} term regardless, which makes it \emph{loose} at high \code{q} and long chains
rather than wrong -- 1959 MB estimated against 1596 MB measured at \code{q = 4},
500 draws.

\strong{The relationship is convex in \code{length_sample}, and the linear fit is
therefore corrected upward.} Peak memory is really a \emph{maximum} over phases --
sampling, rotation, evaluation -- and which phase binds depends on the chain
length: the sampler dominates short chains, the evaluation dominates long
ones. Measured, the peak per MB of retained draws rises from 1.35 (30 to 120
draws) through 1.97 (120 to 240) to 3.94 (240 to 500), so no single slope fits
it. Refitting the line over the whole range does not help: it still
under-predicts at 500 draws while over-predicting the middle by 15\%.

Under-prediction is the harmful direction here -- it hands out too many
workers and the run dies hours in -- so the linear fit is multiplied by
\code{MEM_SAFETY_FACTOR}, chosen as the largest measured/fitted ratio over the
calibration points. That makes the estimate an \strong{upper bound} across the
measured range rather than a best fit, at the cost of over-estimating short
chains by around a third. Short chains are cheap; the tool exists for the long
ones.

This was found the hard way. Before the correction the model predicted 1128 MB
at 500 draws against \strong{1538 MB measured} -- 27\% low. That is very likely why
the 2019-2020 sweep lost two dates to \code{R_Calloc} out-of-memory: five workers
were allocated against a budget computed from the under-estimate. I had
attributed those failures to contention with a concurrent \verb{R CMD check}, which
was at best only part of it.
}

\section{Calibration}{
Fitted to eight fits measured one per fresh R process (the GC high-water mark
is per process, so several in one session would report only their maximum) at
\code{n = 53}, \code{t = 1559}, \code{s = 22}:\tabular{lll}{
   \code{q} \tab \code{length_sample} \tab measured peak \cr
   1 \tab 30 \tab 465 MB \cr
   2 \tab 30 \tab 604 MB \cr
   3 \tab 30 \tab 745 MB \cr
   4 \tab 30 \tab 916 MB \cr
   2 \tab 120 \tab 686 MB \cr
   2 \tab 240 \tab 846 MB \cr
   2 \tab 500 \tab 1538 MB \cr
   4 \tab 500 \tab 1596 MB \cr
}


The line is fitted on the six short/mid points and then scaled by
\code{MEM_SAFETY_FACTOR} so that it also covers the two long-chain points, which are
the settings the sweeps actually use. \code{q = 2} at 500 draws is the binding one
(covered at ratio 1.01); \code{q = 4} at 500 draws is covered with 23\% to spare, for
the reason given above.

Validated out of sample against a fit on a \strong{different} dataset and a
different version of the code (\code{n = 43}, \code{t = 1464}, \code{q = 2},
\code{length_sample = 200}): 744 MB measured, covered.

The constants are specific to this package's samplers; they were measured on
x86_64 Windows and will drift if the samplers' allocation pattern changes.
\code{dev/calibrate-memory.R} regenerates them.

Calibrated on \code{\link[=fcast_dfm]{fcast_dfm()}}. \code{\link[=ind_dfm]{ind_dfm()}} is a different sampler with no
post-hoc rotation and no packed draw matrix, and has \strong{not} been measured
here.
}

\examples{
# from dimensions
dfm_memory(n = 53, t = 1535, s = 22, q = 2, length_sample = 500)

# the whole point: how many workers fit in 24 GB
dfm_workers(n = 53, t = 1535, s = 22, q = 4, length_sample = 500,
            available_mb = 24 * 1024)

# from the data itself
data(data_ch_dataset_test)
dfm_memory(flows = data_ch_dataset_test$flows,
           stocks = data_ch_dataset_test$stocks,
           q = 2, length_sample = 1000)

}
\seealso{
\code{\link[=fcast_dfm]{fcast_dfm()}}

Other model fitting functions: 
\code{\link{fcast_dfm}()},
\code{\link{ind_dfm}()},
\code{\link{run_fcast}()}
}
\concept{model fitting functions}
