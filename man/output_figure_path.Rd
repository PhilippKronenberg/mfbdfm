% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics-config.R
\name{output_figure_path}
\alias{output_figure_path}
\title{Build the full path for a figure output file}
\usage{
output_figure_path(filename, figures_dir)
}
\arguments{
\item{filename}{File name (without directory).}

\item{figures_dir}{Directory the figure belongs in (e.g.
\code{wai_sample_config()$figures_dir}). Must already exist; not created here.}
}
\value{
The full file path.
}
\description{
Purely a path: this creates nothing on disk. \code{figures_dir} is expected to
exist already — the analytics preludes (\verb{analysis/5_plots/_setup.R},
\verb{analysis/fcast/_setup.R}) create the whole output tree up front, and so does
any caller that has written a table or result through
\code{\link[=write_table_output]{write_table_output()}}/\code{\link[=save_result_output]{save_result_output()}}.
}
\details{
It did briefly create the directory, on the reasoning that the returned path
is written to immediately afterwards. That was a mistake worth recording: a
path builder with a side effect surprised a test that passed it the relative
path \code{"outputs/figures"}, which had been harmless for as long as the function
was a plain \code{\link[=file.path]{file.path()}} call, and the suite started leaving
\code{tests/testthat/outputs/figures} behind in the source tree.
}
\examples{
output_figure_path("history.pdf", figures_dir = file.path(tempdir(), "figures"))
}
