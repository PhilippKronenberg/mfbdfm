% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-input.R
\name{mfbdfm_data}
\alias{mfbdfm_data}
\title{Assemble and check the input data for a dynamic factor model}
\usage{
mfbdfm_data(data, meta, target = NULL, aggregate = c("mean", "sum"))
}
\arguments{
\item{data}{The input series, in any of the forms above.}

\item{meta}{A data frame with one row per series and at least the columns
\code{series} and \code{type} (\code{"flow"} or \code{"stock"}). May carry further columns
(\code{label}, \code{source}, ...), which are kept but unused. A \code{frequency}
column, if present, declares the frequency instead of inferring it.}

\item{target}{Optional character. If given, checked to be present among the
series, so a typo surfaces here rather than deep in the sampler. It is
stored on the object and used as the default \code{target} by \code{\link[=ind_dfm]{ind_dfm()}} and
\code{\link[=fcast_dfm]{fcast_dfm()}}.}

\item{aggregate}{How to combine observations that fall into the same
48-week slot when a weekly or daily series is put on the grid, either
\code{"mean"} (the default) or \code{"sum"}. See the "Weekly and daily series"
section.}
}
\value{
An object of class \code{"mfbdfm_data"}: a list with \code{flows}, \code{stocks}
(named lists of \code{ts}, ready to pass to a model), \code{meta} (the resolved
per-series table, with the frequency and observation count actually used,
plus \code{frequency_in} where a series was converted) and \code{target}.
}
\description{
Converts the data you have into the form the models need, and -- more
importantly -- makes the flow/stock classification and the inferred
frequencies visible so they can be checked \emph{before} committing to a run
that takes minutes to hours.
}
\section{Why this exists}{
\code{\link[=ind_dfm]{ind_dfm()}} and \code{\link[=fcast_dfm]{fcast_dfm()}} take \code{flows} and \code{stocks} as two separate
named lists of \code{ts} objects. That shape is necessary -- mixed frequencies
cannot sit in one rectangular table without padding -- but it means each
series' type is expressed by \emph{which argument it was passed in}, so there is
nowhere to inspect or correct it.

That matters because the type selects the temporal aggregation weights:

\describe{
\item{\code{"flow"}}{A quantity accumulated over the period (GDP, sales,
traffic counts). Gets the triangular Mariano-Murasawa weights.}
\item{\code{"stock"}}{A level observed at a point, or an average (a price
index, VIX, a sentiment balance). Gets simple averaging weights.}
}

For series at the \strong{highest} frequency in the dataset the two sets of
weights are identical, so the choice cannot matter there. For lower-frequency
series they differ substantially (measured on a weekly-highest dataset: a
monthly series gets 7 nonzero lags as a flow against 4 as a stock, a
quarterly one 23 against 12), and misclassifying one silently changes your
results. \code{mfbdfm_data()} therefore requires \code{type} for exactly the series
where it can change the answer, and \code{print()} shows the classification back
to you.
}

\section{What it accepts}{
\code{data} may be
\itemize{
\item a \strong{long} data frame with columns \code{series}, \code{date} (or \code{time}) and
\code{value} -- handles ragged histories and mixed frequency with no padding;
\item a \strong{wide} data frame with a date/time column and one column per
series (leading and trailing \code{NA}s are trimmed per series, so a ragged
panel does not become a padded one);
\item an \code{mts} (multivariate \code{ts}), for a single-frequency dataset;
\item a named \strong{list of \code{ts}} objects, which is what the models use
internally, so this is a pass-through with validation.
}
}

\section{Frequency}{
For data frames the frequency is inferred from each series' own date
spacing and reported by \code{print()}, so an inference you did not intend is
visible rather than silent. Only the frequencies this package models are
recognised -- 4 (quarterly), 12 (monthly), 48, 52 (weekly) and 365 (daily);
anything else is an error rather than a guess, because a wrong frequency
silently corrupts the temporal aggregation.

Frequency 48 is the four-weeks-per-month grid used by the Weekly Activity
Index, on which observations fall on the 7th, 14th, 21st and 28th of each
month (see \code{\link[=dec2week]{dec2week()}}). It has the same ~7-day spacing as a true weekly
series, so it is identified by that day-of-month convention; a series with
7-day steps that does not follow it is taken to be frequency 52. Supply a
\code{frequency} column in \code{meta} to declare the frequency outright and skip
the inference. Declaring one that contradicts an input that is already a
\code{ts} is an error rather than a silent override.
}

\section{Weekly and daily series are put on the 48-week grid}{
48 is not an arbitrary house convention -- it is the grid the models are
built on, and a series at a finer frequency \strong{silently loses most of the
lower-frequency data}.

\code{\link[=prepare_data]{prepare_data()}} shifts each series' observations to the end of their period
by \code{(max(freq)/frequency(x) - 1)/max(freq)} and then matches them onto a
\code{1/max(freq)} grid by an exact join. When the highest frequency is 48 that
ratio is a whole number for monthly (4) and quarterly (12) series, so every
observation lands on a grid point. When a frequency-52 series raises the
maximum to 52, \code{52/12} is 4.33: the shifted monthly observations no longer
fall on the grid, the join does not match them, and they are recorded as
missing. Measured on a 20-quarter / 60-month / 260-week panel, the monthly
series retained \strong{20 of its 60 observations} -- and the fit completed
without any error or warning.

So any series arriving at a frequency finer than 48 -- 52 (weekly) or 365
(daily) -- is aggregated onto the 48-week grid with \code{\link[=daily2weekly]{daily2weekly()}}, which
is the same function the analysis scripts use for this. A \code{message()} names
the series converted, \code{print()} shows the original frequency alongside the
new one, and \code{meta$frequency_in} records it.

Note this is one of the things you get by going through \code{mfbdfm_data()}:
passing \code{flows}/\code{stocks} straight to a model does no such conversion, and a
frequency-52 series there will still quietly cost you most of your monthly
observations.

\code{aggregate} chooses how observations sharing a slot are combined (roughly
four slots a year receive two weekly observations). The default \code{"mean"}
matches \code{\link[=daily2weekly]{daily2weekly()}} and is the per-period rate, which is what these
models want, since they are estimated on growth rates.
}

\examples{
# from a long data frame
long <- data.frame(
  series = rep(c("a_flow", "b_stock"), each = 24),
  date = rep(seq(as.Date("2020-01-01"), by = "month", length.out = 24), 2),
  value = rnorm(48))
meta <- data.frame(series = c("a_flow", "b_stock"),
                   type = c("flow", "stock"))
d <- mfbdfm_data(long, meta)
d

# a true weekly series is aggregated onto the 48-week grid the models use,
# and the original frequency is reported
wk <- seq(as.Date("2020-01-06"), by = "week", length.out = 157)
d <- mfbdfm_data(
  rbind(data.frame(series = "weekly", date = wk, value = rnorm(length(wk))),
        data.frame(series = "monthly",
                   date = seq(as.Date("2020-01-01"), by = "month",
                              length.out = 36),
                   value = rnorm(36))),
  data.frame(series = c("weekly", "monthly"), type = c("flow", "stock")))
d$meta

# from the shipped dataset, which is already a list of `ts`
data(data_ch_dataset_test)
series <- c(data_ch_dataset_test$flows, data_ch_dataset_test$stocks)
meta <- data.frame(
  series = names(series),
  type = rep(c("flow", "stock"), lengths(data_ch_dataset_test)))
mfbdfm_data(series, meta, target = "ch.seco.gdp.real.gdp.ssa")

}
\seealso{
\code{\link[=ind_dfm]{ind_dfm()}}, \code{\link[=fcast_dfm]{fcast_dfm()}}, \code{\link[=create_inventory]{create_inventory()}}, \code{\link[=prepare_data]{prepare_data()}}

Other data preparation functions: 
\code{\link{create_inventory}()},
\code{\link{prepare_data}()}
}
\concept{data preparation functions}
