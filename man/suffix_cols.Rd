% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics-cor.R
\name{suffix_cols}
\alias{suffix_cols}
\title{Suffix the lag columns of a correlation or error table}
\usage{
suffix_cols(df, suffix)
}
\arguments{
\item{df}{A data frame with zero or more columns named \verb{Lag_*}.}

\item{suffix}{Character, appended to each lag column name after an underscore.}
}
\value{
\code{df} with its \verb{Lag_*} columns renamed. A data frame with no such
columns is returned unchanged.
}
\description{
Renames every \verb{Lag_*} column to \verb{Lag_*_<suffix>}, leaving all other columns
untouched. Used when several tables that share a lag layout are put side by
side and their columns would otherwise collide -- one per frequency (\code{"QoQ"},
\code{"YoY"}) or per aggregation method (\code{"mean"}, \code{"last"}, \code{"lastmonth"}).
}
\details{
Exported because \verb{analysis/5_plots/analytics_in_sample.R} calls it in six
places. It was \verb{@noRd} and therefore invisible outside the package, and
\code{create_combined_latex_table()} carried a second, identical copy defined
inside its own body -- so the script failed with
\verb{could not find function "suffix_cols"} the first time the analytics chain was
run end to end (#23). One definition now, visible to both.
}
\examples{
df <- data.frame(Series = c("WAI", "AR"), Lag_0 = c(1, 2), Lag_1 = c(3, 4))
suffix_cols(df, "QoQ")

# columns that are not lags are left alone, and a frame without any is a no-op
suffix_cols(data.frame(Series = "WAI", value = 1), "QoQ")

}
\concept{evaluation table functions}
