% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fcast_dfm.R
\name{fcast_dfm}
\alias{fcast_dfm}
\title{Estimate a multi-factor mixed-frequency dynamic factor model}
\usage{
fcast_dfm(
  flows = NULL,
  stocks = NULL,
  target,
  p = 1,
  q = 2,
  length_sample = 1000,
  burn_in = 1000,
  thinning = 1,
  plots = FALSE,
  extend = NULL,
  stochastic_volatility = TRUE,
  serial_correlation = TRUE,
  ncores = NULL,
  priors = dfm_priors("fcast_dfm")
)
}
\arguments{
\item{flows}{Either an \code{\link[=mfbdfm_data]{mfbdfm_data()}} object carrying all series and
their flow/stock classification, or a named list of \code{ts} objects treated
as flow variables, or
\code{NULL}. Must contain \code{target} if \code{stocks} does not.}

\item{stocks}{Named list of \code{ts} objects treated as stock variables, or
\code{NULL}.}

\item{target}{Character, name of the series of interest (e.g.
\code{"ch.seco.gdp.real.gdp.ssa"}). Must be present in \code{flows} or \code{stocks}.
Does not affect estimation; see Details.}

\item{p}{Integer, number of lags in the factor VAR.}

\item{q}{Integer, number of factors. Must be smaller than the number of
input series.}

\item{length_sample}{Integer, number of posterior draws to keep.}

\item{burn_in}{Integer, number of initial draws to discard.}

\item{thinning}{Integer, keep every \code{thinning}-th draw after burn-in.}

\item{plots}{Logical, if \code{TRUE} draw diagnostic plots of the factors,
stochastic volatility and trace plots during sampling.}

\item{extend}{Numeric or \code{NULL}. If given, the dataset is extended by this
many years with zeros so forecasts can be produced.}

\item{stochastic_volatility}{Logical, include stochastic volatility in the
factor state equation.}

\item{serial_correlation}{Logical, model serial correlation in the
measurement errors. If \code{FALSE}, the autocorrelations are fixed near zero.}

\item{ncores}{Integer or \code{NULL}. Number of cores for the rotation step,
which is run in parallel via \pkg{doParallel} when supplied.}

\item{priors}{Prior specification from \code{\link[=dfm_priors]{dfm_priors()}}. The default
reproduces the published priors exactly. Note that the loading prior
carries the identification -- it must stay diffuse for the post-hoc
rotation to work; see \code{\link[=dfm_priors]{dfm_priors()}}.}
}
\value{
An object of class \code{"fcast_dfm"}: a list with components
\describe{
\item{factor}{\code{ts} matrix of the \code{q} posterior mean factors.}
\item{factor_var}{\code{ts} matrix of the corresponding variances.}
\item{target}{Character, the series named by \code{target}.}
\item{nowcast, nowcast_var}{\code{ts}, posterior mean and variance of the
nowcast for \code{target}, extracted from \code{ncst}.}
\item{pars}{List of posterior means (\code{lambda}, \code{phi}, \code{sigma}, \code{rho},
\code{rho_var}, \code{h}) and the model dimensions (\code{n}, \code{q}, \code{p}, \code{s}, \code{t},
\code{k}).}
\item{ncst}{List with \code{mean} and \code{var}, each a named list of nowcasts
for every input series at its own frequency.}
\item{data}{\code{ts} matrix of the prepared (standardized) data, in which
\code{0} encodes a missing observation.}
\item{data_raw}{The input series, as supplied.}
\item{data_hf}{List with \code{mean} and \code{var}, each a named list of
high-frequency growth-rate estimates for every input series.}
\item{data_augmented}{\code{ts} matrix of the augmented dataset.}
\item{data_augmented_rescaled}{The same, back on each series' original
scale.}
\item{inventory}{Data frame describing the series (see
\code{\link[=create_inventory]{create_inventory()}}).}
\item{target_series}{List collecting everything about \code{target} for
inspection: \code{nowcast} (a data frame of \code{time}, \code{observed}, \code{mean},
\code{lower}, \code{upper} at the target's own frequency) and
\code{high_frequency} (the same columns for the high-frequency growth
estimate). See \link{fcast_dfm_methods}.}
\item{call}{The matched call.}
}
}
\description{
Estimates the Bayesian multi-factor mixed-frequency dynamic factor model
of Eckert, Kronenberg, Mikosch & Neuwirth (2025) by Markov chain Monte
Carlo. Flow and stock indicator series of different frequencies are
combined into \code{q} common factors, from which high-frequency estimates and
nowcasts are derived for every input series.
}
\details{
This is a different model from \code{\link[=ind_dfm]{ind_dfm()}}, not a multi-factor setting of
it. The two differ in how the factors are identified, in their priors,
and in how the autoregressive coefficients are drawn:

\describe{
\item{Identification}{\code{\link[=ind_dfm]{ind_dfm()}} identifies the factor \emph{during} sampling,
by fixing the loading on \code{target} to one and shrinking that series'
measurement error toward zero, so the factor is directly interpretable
as the target's growth rate. \code{fcast_dfm()} instead samples an
unidentified model and resolves the rotational indeterminacy
afterwards: every draw is rotated onto a common reference by
orthogonal Procrustes, then one global rotation is chosen to make the
average loading matrix close to its varimax rotation.}
\item{Priors}{\code{fcast_dfm()} uses uninformative priors throughout; no
series is treated specially.}
\item{Factor dynamics}{The factors follow a VAR(\code{p}) whose coefficients
are drawn by Metropolis-Hastings with a stationarity constraint,
rather than the conjugate Gibbs step used by \code{\link[=ind_dfm]{ind_dfm()}}. A single
stochastic volatility path is shared by all \code{q} factors.}
}

Because identification differs, \code{fcast_dfm(q = 1)} is \strong{not} equivalent to
\code{\link[=ind_dfm]{ind_dfm()}}. Use \code{\link[=ind_dfm]{ind_dfm()}} for the target-anchored single-factor model of
Kronenberg (2026), and \code{fcast_dfm()} for the multi-factor model.
}
\section{Maturity}{
This function is \strong{experimental}. It reproduces the published sampler and
is covered by structural tests, but it has not yet been validated by
simulation recovery -- generating data from a known \code{q}-factor process and
checking that the estimated factors and loadings recover it up to rotation.
Until that exists, treat multi-factor results (\code{q > 1}) as provisional.
\code{\link[=ind_dfm]{ind_dfm()}} is the settled entry point.

\code{target} does not enter the estimation. It names the series whose nowcast
is surfaced at the top level of the return value for convenience; results
for every series remain available in \code{ncst} and \code{data_hf}.
}

\examples{
# \donttest, not \dontrun: this works on the shipped data, it is only slow -
# the post-hoc rotation step scales with the number of retained draws.
\donttest{
data(data_ch_dataset_test)
target <- "ch.seco.gdp.real.gdp.ssa"
flows <- lapply(data_ch_dataset_test$flows[c(target, "SWISSMI")],
                stats::window, start = 2021)
stocks <- lapply(data_ch_dataset_test$stocks[1:2],
                 stats::window, start = 2021)
set.seed(1)
fit <- fcast_dfm(flows = flows, stocks = stocks, target = target,
                 q = 2, length_sample = 20, burn_in = 5)
fit
}

}
\references{
Eckert, F., Kronenberg, P., Mikosch, H., & Neuwirth, S. (2025).
Tracking economic activity with alternative high-frequency data.
\emph{Journal of Applied Econometrics}, 40(3), 270-290.

Kronenberg, P. (2026). A high-frequency GDP indicator for
Switzerland. \emph{Swiss Journal of Economics and Statistics}, 162, 10.
\doi{10.1186/s41937-026-00157-w}
}
\seealso{
\code{\link[=ind_dfm]{ind_dfm()}} for the single-factor, target-anchored model.

Other model fitting functions: 
\code{\link{ind_dfm}()}
}
\concept{model fitting functions}
