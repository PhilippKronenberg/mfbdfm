% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ind_dfm.R
\name{ind_dfm}
\alias{ind_dfm}
\title{Estimate a high-frequency dynamic factor model}
\usage{
ind_dfm(
  flows = NULL,
  stocks = NULL,
  target,
  p = 1,
  length_sample = 10000,
  burn_in = 1000,
  thinning = 1,
  plots = FALSE,
  extend_to = NULL,
  stochastic_volatility = TRUE,
  serial_correlation = TRUE,
  priors = dfm_priors("ind_dfm")
)
}
\arguments{
\item{flows}{Named list of \code{ts} objects treated as flow variables. Must
contain \code{target}.}

\item{stocks}{Named list of \code{ts} objects treated as stock variables.}

\item{target}{Character, name of the low-frequency target series in
\code{flows} (e.g. \code{"ch.seco.gdp.real.gdp.ssa"}).}

\item{p}{Integer, number of factor lags in the factor state equation.}

\item{length_sample}{Integer, number of posterior draws to keep.}

\item{burn_in}{Integer, number of initial draws to discard.}

\item{thinning}{Integer, keep every \code{thinning}-th draw after burn-in.}

\item{plots}{Logical, if \code{TRUE} draw base-graphics diagnostic plots of
the data and of factor/volatility convergence during sampling.}

\item{extend_to}{Numeric (decimal time) or \code{NULL}. If beyond the sample
end, the dataset is extended with zeros so forecasts can be produced.}

\item{stochastic_volatility}{Logical. If \code{TRUE} (default) the factor
innovation variance follows a stochastic volatility process. If \code{FALSE}
it is a single constant variance, \strong{still estimated} rather than fixed
-- see \verb{@details}.}

\item{serial_correlation}{Logical. If \code{TRUE} (default) the measurement
errors are allowed to be serially correlated and their autocorrelations
are drawn. If \code{FALSE} they are held at (effectively) zero.}

\item{priors}{Prior specification from \code{\link[=dfm_priors]{dfm_priors()}}. The default
reproduces the published priors exactly. Note that two of them -- the
target's measurement-error variance and serial correlation -- carry the
identification rather than being tuning knobs; see \code{\link[=dfm_priors]{dfm_priors()}}.}
}
\value{
An object of class \code{"ind_dfm"}: a list with components
\describe{
\item{factor}{\code{ts}, posterior mean of the annualized activity factor.}
\item{factor_var}{\code{ts}, posterior variance of the factor.}
\item{index}{\code{ts}, posterior mean of the cumulated activity index.}
\item{nowcast}{\code{ts}, posterior mean nowcast of the target series.}
\item{nowcast_var}{\code{ts}, posterior variance of the nowcast.}
\item{target}{Character, the target series name.}
\item{pars}{List of posterior parameter means (\code{h}, \code{lambda}, \code{phi},
\code{sigma}, \code{omega}, \code{rho}, \code{rho_var}).}
\item{data}{\code{ts} matrix of the prepared (standardized) data.}
\item{data_augmented}{\code{ts} matrix of the augmented dataset.}
\item{inventory}{Data frame describing the series (see
\code{\link[=create_inventory]{create_inventory()}}).}
\item{call}{The matched call.}
}
}
\description{
Estimates the Bayesian mixed-frequency dynamic factor model behind the
Swiss Weekly Activity Index (WAI) by Markov chain Monte Carlo (Gibbs)
sampling. Flow and stock indicator series of different frequencies are
combined into a single weekly activity factor that is coherent with the
low-frequency target series (typically quarterly real GDP), from which
weekly GDP nowcasts are derived.
}
\details{
The factor is identified by fixing its loading on \code{target} to one and
shrinking the target's measurement-error variance and autocorrelation
toward zero (informative priors), so the extracted factor closely
tracks the observed growth rate of \code{target} rather than being merely
correlated with it. This resolves the usual scale/sign indeterminacy
of dynamic factor models and yields a directly interpretable
high-frequency proxy for the target series (see Kronenberg 2026,
Sect. 2.4). All other series are standardized and enter with
uninformative priors. Missing and lower-frequency observations are
estimated as latent states via data augmentation; the factor state
equation includes stochastic volatility, and measurement errors are
quasi-differenced to remove serial correlation (see \verb{@references}).

Because the factor's scale is pinned by the loading restriction, the
factor innovation variance is a free parameter that the data must
determine. Setting \code{stochastic_volatility = FALSE} therefore does \strong{not}
fix that variance: it replaces the time-varying volatility path with a
single constant variance which is still estimated, drawn from its
conjugate inverse-gamma posterior each iteration. (This differs from
\code{\link[=fcast_dfm]{fcast_dfm()}}, whose loadings are unrestricted and whose innovation
variance consequently carries the identification and \emph{is} fixed at one
when its stochastic volatility is switched off. The two models pin the
scale in different places, so switching the same option off means
something different in each.)
}
\examples{
\donttest{
data(data_ch_dataset_test)
target <- "ch.seco.gdp.real.gdp.ssa"
flows <- lapply(data_ch_dataset_test$flows[c(target, "SWISSMI")],
                stats::window, start = 2018)
stocks <- lapply(data_ch_dataset_test$stocks[1:2],
                 stats::window, start = 2018)
set.seed(1)
fit <- ind_dfm(flows = flows, stocks = stocks, target = target,
             length_sample = 50, burn_in = 10)
fit$nowcast
}

}
\references{
Kronenberg, P. (2026). A high-frequency GDP indicator for
Switzerland. \emph{Swiss Journal of Economics and Statistics}, 162, 10.
\doi{10.1186/s41937-026-00157-w}

Eckert, F., Kronenberg, P., Mikosch, H., & Neuwirth, S. (2025).
Tracking economic activity with alternative high-frequency data.
\emph{Journal of Applied Econometrics}, 40(3), 270-290.
}
\seealso{
\code{\link[=fcast_dfm]{fcast_dfm()}} for the multi-factor model, \code{\link[=dfm_priors]{dfm_priors()}} to vary
the priors, and \link{ind_dfm_methods} for the \code{print}, \code{summary}, \code{plot},
\code{coef}, \code{fitted}, \code{residuals} and \code{as.data.frame} methods.
}
