% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/web-export.R
\name{gdp_web_series}
\alias{gdp_web_series}
\title{Published GDP on the same scale as the exported WAI series}
\usage{
gdp_web_series(
  vintage = "latest",
  base_date = as.Date("2019-10-01"),
  start_date = as.Date("1990-01-01"),
  end_date = NULL,
  ...
)
}
\arguments{
\item{vintage}{Which publication vintage to use: \code{"latest"} (default) for the
most recent column, or the name of a vintage column.}

\item{base_date}{\code{Date} (or string) naming the quarter the level index is
rebased to. Defaults to 2019-10-01, matching \code{wai_index}, whose base is the
last quarter of 2019.}

\item{start_date, end_date}{Optional \code{Date} bounds on the quarters returned.
The growth rates are always computed on the full available history and the
window applied afterwards, so the first returned quarter carries a real
\code{qoq} and \code{yoy} rather than the \code{NA} a post-trim lag would leave.}

\item{...}{Passed to \code{\link[=get_real_time_gdp_vintages]{get_real_time_gdp_vintages()}}, e.g. the two file-path
arguments.}
}
\value{
A data frame with columns \code{time} (Date, quarter start), \code{qoq}, \code{yoy}
and \code{index}. Ready to hand to \code{\link[=export_wai_web]{export_wai_web()}}'s \code{gdp} argument.
}
\description{
Builds official GDP as quarter-on-quarter annualised growth, year-over-year
growth and a rebased level index, all on the units \code{\link[=export_wai_web]{export_wai_web()}}
publishes, so the three can be plotted against their WAI counterparts on one
axis.
}
\details{
\code{\link[=get_real_time_gdp_vintages]{get_real_time_gdp_vintages()}} returns \code{"quarterly"} as a log difference and
\code{"annual"} as a fraction, neither of which is the annualised percentage the
WAI series use. Rather than convert at each call site — which is how an
unconverted log difference nearly reached the published dashboard, wrong by a
factor of roughly 400 — the conversion lives here, in one tested place.

All three series are derived from the \emph{same} vintage levels, so they are
mutually consistent by construction:

\describe{
\item{\code{qoq}}{\verb{((level_t / level_\{t-1\})^4 - 1) * 100}}
\item{\code{yoy}}{\verb{(level_t / level_\{t-4\} - 1) * 100}}
\item{\code{index}}{\code{100 * level_t / level_base}}
}
}
\examples{
\donttest{
gdp <- gdp_web_series()
tail(gdp)
}
}
\seealso{
\code{\link[=get_real_time_gdp_vintages]{get_real_time_gdp_vintages()}} for the untransformed vintages.
}
